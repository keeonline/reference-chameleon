name: Pull request

on:
  pull_request:
  
jobs:
  salutation:
      runs-on: ubuntu-24.04
      steps:
        - name: salutation
          run: |
            echo This is on_pull_request for chameleon service

        - name: deploy IaC
          run: |
            echo "placeholder"

# This should trigger a 'repository_dispatch' to execute the IaC with a given sha and set of args ???

# add processing to build local image and run intergration tests using docker compose