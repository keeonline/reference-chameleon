name: Pull request

on:
  pull_request:
  
jobs:
  salutation:
      runs-on: ubuntu-24.04
      steps:
        - name: salutation
          run: |
            echo This is on_pull_request for chameleon service
